# Goal: to build CI/CD pipeline (dummi).
name: CICD demo

on: 
  push:
 
jobs:
  # CI
  code:
    runs-on: ubuntu-22.04
    steps:
      - name: Clone the code
        uses: actions/checkout@v4
  
  build: 
    needs: code
    runs-on: ubuntu-22.04
    steps:
      - name: Build the code the code with Docker  
        run: echo "Build comple"
  test:
    needs: build
    runs-on: ubuntu-22.04
    steps:
      - name: test the Application  
        run: echo "test passed" 
  # CD
  deploy:
    needs: [code,build,test]
    runs-on: ubuntu-22.04
    steps:
      - name: Deploy app on server  
        run: echo "App Deployed" 
  




